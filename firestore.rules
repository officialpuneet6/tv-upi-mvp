rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tv_notifications/{document=**} {
      allow read, write: if true; // MVP: Open access for simplicity
    }
  }
}
